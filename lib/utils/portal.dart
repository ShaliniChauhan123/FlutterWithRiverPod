enum PortalType {
  general,
  community,
  games,
  work,
  professional,
  learning,
  app,
  brand,
  creator,
  buysell,
}

enum PortalPrivacy {
  public,
  private,
}

enum RequestStatus {
  hasRequested,
  notRequested,
}

enum MemberRequestStatus {
  accepted,
  pending,
  rejected,
}
